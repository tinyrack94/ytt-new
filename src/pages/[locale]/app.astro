---
import YTTApp from '@/routes/App.astro';
import Layout from '@/layouts/Layout.astro';
import { buildStaticPaths } from 'astro-react-i18next/utils';

export async function getStaticPaths() {
  return buildStaticPaths();
}
---

<Layout>
  <YTTApp />
</Layout>
